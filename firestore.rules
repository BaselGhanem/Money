rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /moneyUsers/{userId}/apps/{appId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
