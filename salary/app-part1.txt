(()=>{`use strict`;
const S=`salary_manager_v2`,K=`salary_manager_unlocked`,H=`954477f657208b27556a562e438e227cc7ac618721a5d8a18068feb6c2c83bad`,M=new Date().toISOString().slice(0,7),A=[`Arab Bank`,`Reflect`,`Etihad`,`Zain Cash`,`Blink`,`Rajihi`,`Jordan Bank`,`Qawn`,`U Wallet`],D=[[`إيداع الراتب من شركتي إلى بنكي`,1200,`income`,`Arab Bank`,`دخل`],[`خصم القرض`,436,`expense`,`Arab Bank`,`قرض`],[`كهرباء أهلي`,15,`expense`,`Arab Bank`,`عائلة`],[`إنترنت أمنية`,10,`expense`,`Arab Bank`,`عائلة`],[`كهرباء بيتي`,35,`expense`,`Arab Bank`,`فواتير`],[`إنترنت زين`,23,`expense`,`Arab Bank`,`فواتير`],[`مصروف عرين`,75,`expense`,`Arab Bank`,`شخصي`],[`مصروف باسل`,100,`expense`,`Arab Bank`,`شخصي`],[`مصروف البيت`,150,`expense`,`Arab Bank`,`منزل`],[`مصروف المول`,100,`expense`,`Arab Bank`,`منزل`],[`بنزين`,100,`expense`,`Arab Bank`,`مواصلات`],[`مصري`,9,`expense`,`Arab Bank`,`فواتير`],[`أبو النور`,5,`expense`,`Arab Bank`,`فواتير`],[`فاتورة مياه`,5,`expense`,`Arab Bank`,`فواتير`],[`مايكروسوفت`,6,`expense`,`Arab Bank`,`اشتراكات`],[`بطاقات`,25,`expense`,`Arab Bank`,`بطاقات`]],$=i=>document.getElementById(i),id=()=>crypto.randomUUID?.()||`i_${Date.now()}_${Math.random()}`,n=v=>Number.isFinite(+v)?+v:0,e=v=>String(v??``).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),ml=v=>new Intl.DateTimeFormat(`ar-JO`,{month:`long`,year:`numeric`}).format(new Date(`${v}-01T12:00:00`));
const fresh=()=>({activeMonth:M,settings:{siteName:`راتبي`,subtitle:`Financial OS`,userName:`باسل`,currency:`د.أ`,theme:`dark`,accent:`#19c7bd`,motion:true,privacy:false,defaultAccount:`Arab Bank`,budget:0},accounts:Object.fromEntries(A.map((x,i)=>[x,{opening:0,active:true,order:i,symbol:x.slice(0,2)}])),months:{[M]:{items:D.map(r=>({id:id(),name:r[0],amount:r[1],type:r[2],account:r[3],category:r[4],recurring:true,date:``,notes:``})),transfers:[]}}});
let st=(()=>{try{const x=JSON.parse(localStorage.getItem(S));if(!x?.months)return fresh();const b=fresh();x.settings={...b.settings,...x.settings};x.accounts=x.accounts||b.accounts;Object.entries(x.accounts).forEach(([k,v],i)=>x.accounts[k]={opening:n(v.opening),active:v.active!==false,order:n(v.order??i),symbol:v.symbol||k.slice(0,2)});x.activeMonth=x.activeMonth||M;Object.values(x.months).forEach(m=>{m.items=Array.isArray(m.items)?m.items:[];m.transfers=Array.isArray(m.transfers)?m.transfers:[]});return x}catch{return fresh()}})(),rt=`all`,sort={key:`date`,dir:`desc`};
function ensure(m){if(st.months[m])return;const p=Object.keys(st.months).filter(x=>x<m).sort().pop(),src=p?st.months[p].items.filter(x=>x.recurring):D.map(r=>({name:r[0],amount:r[1],type:r[2],account:r[3],category:r[4],recurring:true,date:``,notes:``}));st.months[m]={items:src.map(x=>({...x,id:id(),date:x.date?`${m}-${x.date.slice(-2)}`:``})),transfers:[]}}
const mon=()=>{ensure(st.activeMonth);return st.months[st.activeMonth]},acs=()=>Object.entries(st.accounts).filter(([,v])=>v.active!==false).sort((a,b)=>a[1].order-b[1].order),money=v=>`${n(v).toLocaleString(`en-US`,{minimumFractionDigits:2,maximumFractionDigits:2})} ${st.settings.currency}`,tot=m=>{const i=m.items.filter(x=>x.type===`income`).reduce((s,x)=>s+n(x.amount),0),o=m.items.filter(x=>x.type===`expense`).reduce((s,x)=>s+n(x.amount),0);return{income:i,expense:o,remaining:i-o}},bal=(a,m=mon())=>{let b=n(st.accounts[a]?.opening);m.items.forEach(x=>{if(x.account===a)b+=x.type===`income`?n(x.amount):-n(x.amount)});m.transfers.forEach(x=>{if(x.from===a)b-=n(x.amount);if(x.to===a)b+=n(x.amount)});return b};
function all(){const r=[];Object.entries(st.months).forEach(([month,m])=>{m.items.forEach(x=>r.push({...x,month,toAccount:``}));m.transfers.forEach(x=>r.push({id:x.id,name:x.notes||`حوالة داخلية`,amount:n(x.amount),type:`transfer`,account:x.from,toAccount:x.to,category:`حوالة`,date:x.date||``,month}))});return r}
function apply(){const t=st.settings.theme===`system`?(matchMedia(`(prefers-color-scheme: light)`).matches?`light`:`dark`):st.settings.theme;document.body.classList.toggle(`light`,t===`light`);document.body.classList.toggle(`no-motion`,!st.settings.motion);document.body.classList.toggle(`privacy`,!!st.settings.privacy);document.documentElement.style.setProperty(`--accent`,st.settings.accent);$(`brandName`).textContent=st.settings.siteName;$(`brandSubtitle`).textContent=st.settings.subtitle;$(`welcomeTitle`).textContent=`أهلًا، ${st.settings.userName}`;document.title=st.settings.siteName}
function save(msg=``){localStorage.setItem(S,JSON.stringify(st));apply();render();if(msg)toast(msg)}
function opts(){const o=acs().map(([x])=>`<option value="${e(x)}">${e(x)}</option>`).join(``);[`transactionAccount`,`transferFrom`,`transferTo`,`settingDefaultAccount`].forEach(i=>{const z=$(i),v=z.value;z.innerHTML=o;if([...z.options].some(q=>q.value===v))z.value=v});[`txAccount`,`reportAccount`].forEach(i=>{const z=$(i),v=z.value||`all`;z.innerHTML=`<option value="all">كل الحسابات</option>${o}`;z.value=[...z.options].some(q=>q.value===v)?v:`all`})}
const empty=t=>`<div class="empty">${e(t)}</div>`,card=(manager=false)=>Object.entries(st.accounts).sort((a,b)=>a[1].order-b[1].order).map(([x,v])=>`<article class="accountCard" data-account-card="${e(x)}" ${manager?`data-edit-account="${e(x)}"`:``}><div class="accountTop"><div class="accountLogo">${e(v.symbol)}</div><span class="badge">${v.active!==false?`نشط`:`مؤرشف`}</span></div><h3>${e(x)}</h3><div class="bal moneyValue">${money(bal(x))}</div><div class="open"><span>الرصيد الافتتاحي</span><strong class="moneyValue">${money(v.opening)}</strong></div></article>`).join(``);
function row(x){const tr=x.type===`transfer`,label=tr?`حوالة`:x.type===`income`?`دخل`:`مصروف`;return `<article class="tx"><div class="txIcon ${x.type}">${tr?`⇄`:x.type===`income`?`↓`:`↑`}</div><div class="txMain"><strong>${e(x.name)}</strong><span>${e(x.date||`بدون تاريخ`)}</span><div class="badges"><span class="badge">${label}</span><span class="badge">${e(x.account||x.from||``)}${tr?` ← ${e(x.toAccount||x.to||``)}`:``}</span>${x.category?`<span class="badge">${e(x.category)}</span>`:``}</div></div><div class="amount ${x.type} moneyValue">${tr?``:x.type===`income`?`+`:`-`}${money(x.amount)}</div><button class="edit" data-edit-${x.kind}="${e(x.id)}">•••</button></article>`}
function dash(){const m=mon(),t=tot(m),rate=t.income?Math.round(t.expense/t.income*100):0;$(`heroMonth`).textContent=ml(st.activeMonth);$(`heroBalance`).textContent=money(t.remaining);$(`incomeTotal`).textContent=money(t.income);$(`expenseTotal`).textContent=money(t.expense);$(`spendRate`).textContent=`${rate}%`;$(`movementCount`).textContent=m.items.length+m.transfers.length;$(`sideTotal`).textContent=money(acs().reduce((s,[x])=>s+bal(x),0));$(`dashboardAccounts`).innerHTML=card();const r=[...m.items.map(x=>({...x,kind:`item`})),...m.transfers.map(x=>({...x,type:`transfer`,kind:`transfer`,name:x.notes||`حوالة داخلية`,account:x.from,toAccount:x.to}))].sort((a,b)=>(b.date||``).localeCompare(a.date||``)).slice(0,6);$(`recentList`).innerHTML=r.length?r.map(row).join(``):empty(`لا توجد حركات بعد.`)}
function txs(){const q=$(`txSearch`).value.toLowerCase(),ty=$(`txType`).value,a=$(`txAccount`).value,r=$(`txRecurring`).value,x=mon().items.filter(v=>(!q||`${v.name} ${v.category} ${v.notes}`.toLowerCase().includes(q))&&(ty===`all`||v.type===ty)&&(a===`all`||v.account===a)&&(r===`all`||(r===`recurring`?v.recurring:!v.recurring))).sort((a,b)=>(b.date||``).localeCompare(a.date||``));$(`transactionContext`).textContent=a===`all`?`كل الدخل والمصاريف.`:`حركات حساب ${a}.`;$(`txList`).innerHTML=x.length?x.map(v=>row({...v,kind:`item`})).join(``):empty(`لا توجد نتائج.`)}
function trs(){const x=[...mon().transfers].sort((a,b)=>(b.date||``).localeCompare(a.date||``));$(`transferList`).innerHTML=x.length?x.map(v=>row({...v,type:`transfer`,kind:`transfer`,name:v.notes||`حوالة داخلية`,account:v.from,toAccount:v.to})).join(``):empty(`لا توجد حوالات.`)}
function reports(){if(!$(`reportFrom`).value)$(`reportFrom`).value=Object.keys(st.months).sort()[0];if(!$(`reportTo`).value)$(`reportTo`).value=st.activeMonth;const q=$(`reportSearch`).value.toLowerCase(),f=$(`reportFrom`).value,t=$(`reportTo`).value,a=$(`reportAccount`).value,r=all().filter(x=>x.month>=f&&x.month<=t&&(rt===`all`||x.type===rt)&&(a===`all`||x.account===a||x.toAccount===a)&&(!q||`${x.name} ${x.account} ${x.toAccount} ${x.category}`.toLowerCase().includes(q))).sort((x,y)=>{const X=x[sort.key]??``,Y=y[sort.key]??``;return(typeof X===`number`?X-Y:String(X).localeCompare(String(Y)))*(sort.dir===`asc`?1:-1)});const sums=[`income`,`expense`,`transfer